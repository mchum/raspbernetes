# Redirect to https
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: https-redirect-middleware
  namespace: default
spec:
  redirectScheme:
    scheme: https
    permanent: true